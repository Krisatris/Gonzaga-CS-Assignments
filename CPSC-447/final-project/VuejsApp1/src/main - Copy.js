

export default class LinkedList {
    constructor() {
        this.head = null;
    }

    /*
    insertAtHead(destination, weight) {
        //let temp = new Node(destination, weight);
        //temp.next = this.head;
        //this.head = temp;
        this.head = 1;
    } */
}